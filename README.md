# HomeWork-.NET-X
В першому тренінгу(Work1), в завданні #4, використано статичні методи, а не статичні класи,
через те, що неможливо було застосувати інтерфейс IRunnable.

В другому тренінгу(Work2), завдання #1 та #2 були об'єднані, для того щоб не дублювати код,
а також в завданні #4 було зроблено окремий клас Task, який об'єднує в собі використання класів Rectangle та Sqaure,
для того щоб було зручно використовувати інтерфейс IRunnable, та метод DrawAll.

